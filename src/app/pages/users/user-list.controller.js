
export class UserListController {
    constructor (
        $rootScope, $scope, $log, $timeout, $location,
        APIService, CookieService
    ) {
        'ngInject';

        (this.init)();
    }

    init() {

    }
}
